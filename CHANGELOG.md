### ✨ v0.12.1 更新日志

#### AI 对话：思考过程、用量统计、流式开关

- 🧠 **显示模型的思考过程：** 会推理的模型（Groq 上的 Qwen 3.6、DeepSeek 等）在写答案之前会先思考很久，而这段思考此前是被直接丢弃的——屏幕上只有一个转圈的加载条，看起来就像流式没生效。现在思考内容实时显示在回答上方：生成中默认展开、逐字流入、自动跟到最新一行，回答出现后折叠成「思考过程」，随对话一起保存，刷新后仍可展开重看。内联的 `<think>…</think>`（Qwen 在 Groq 上的写法）和独立的 `reasoning` / `reasoning_content` 字段（DeepSeek、OpenRouter、Anthropic 的写法）都能识别。
- 📊 **每条回答下方显示这一轮的耗时、token 与速度：** 形如「8.4s · 流式 · 首字 0.9s · 输入 1520 / 输出 96 · 11.4 tok/s」。「首字」是从发出请求到屏幕上出现第一个字的时间，它把"接口慢"和"模型想得久"分开了；速度按扣掉首字等待后的生成时段计算，才是能横向比较模型的数字。token 数向接口索取（OpenAI 兼容端点会附带 `stream_options: {include_usage: true}`），接口不报就不显示这一段，不会编造。
- 🎛️ **AI 设置里新增「流式返回」开关：** 默认开启。关掉后直接走一次性请求，省下接口不支持流式时的那次重发。切换服务商不会重置它——这是对话的偏好，不是模型的属性。
- 📣 **流式降级不再是无声的：** 此前接口拒绝流式时，PageDye 会悄悄改用一次性请求重发一遍，用户看到的只是"文字一次性蹦出来"，和坏掉毫无区别。现在对话里会明说原因，例如「接口返回了 HTTP 400」或「接口返回的是 application/json 而不是事件流」。
- 🗣️ **回答跟随你说话的语言：** 系统提示词是英文写的，此前从未要求模型跟随用户语言，于是中文提问经常收到英文回答。现在明确要求：`reply` 以及主题的名称与说明都用你的语言，字段名、选择器、色值这些不受影响。首条消息是空的（"随便设计一个"）时没有语言线索，则按浏览器语言作答。

#### 🐛 Bug 修复

- 🐛 **修复推理模型的思考内容会污染回答：** 流式解析是在整个缓冲区里搜 `"reply"` 的，而模型的思考里经常会引用 schema 自己的字段名——于是模型的内部思考会被当成回答显示出来；思考里出现的 `{` 也会被当成 JSON 的开头，把整轮答案解析失败。现在思考先被切走，只在剩下的正文里解析。
- 🐛 **修复输入框与两侧圆按钮没有对齐：** 输入框实际高度是 39.5px，附件与发送按钮是 34px，一行内底边对齐之后，两个圆按钮的圆心比输入框文字中线低了将近 3px。现在三者等高，底对齐同时也就是居中对齐；消息变长时按钮仍然跟着最后一行走。设置页那一套皮肤的同一处偏差（39px 对 36px）一并修正。

### ✨ v0.12.0 更新日志

#### 设置页重做：三个大区，取消「高级模式」

- 🗂️ **侧边栏从八九个并列项收成三个大区，每区内部用标签页：** 「网站」下是「已保存网站 / 页面规则 / 分组与批量」，「预设与动效」下是「预设 / 自定义动效」，「数据」下是「备份 / 存储空间」。此前"网站背景""页面规则""批量工具""自定义动效""备份""存储空间"各占一个侧边栏条目，彼此的从属关系只能靠猜。标签页上带数量角标（有几个网站、几条规则、几个自定义动效），不用点进去才知道。
- 🔓 **移除「高级模式」开关，所有功能默认可见：** 此前页面规则、批量工具与分组、自定义动效、每站点的自定义 CSS 与背景选择器、弹窗里的排除元素列表和预设管理入口、开发者控件折叠区，全都藏在这个开关后面——用户要先知道有个开关，才能发现这些功能存在。现在一律直接显示，`__pagedye_advanced_mode__` 这个存储项也一并删除。「调试模式」不再依附于高级模式，作为「设置」页里独立的开发者选项存在。
- 🔗 **旧的深层链接仍然有效：** `#section-custom-effects`、`#section-backup`、`#section-rules`、`#section-appearance`、`#section-ai` 这些已经不再是独立分区的地址，会被映射到新的分区并自动切到对应标签页，弹窗里的「管理自定义动效…」等入口不会失效。
- 🎞️ **标签页切换带方向感：** 面板按标签栏移动的方向左右滑入；分区首次显示时没有"来的方向"，改为整页淡入。勾选「减少界面动画」后这些动画全部静音。
- 💬 **AI 对话页只剩对话本身：** 标题、说明文字、标签页选择器、刷新按钮从页面上撤掉，收进右上角一个「更多」菜单（点击外部或按 Esc 关闭）。对话为空时问候语居中显示，而不是顶在页面最上方。
- 🔑 **AI 设置从独立分区并入「设置」页，并新增两处提示：** 密钥输入框旁边加了「显示密钥」按钮（长串密钥少粘一个字符，遮住时根本看不出来），分组标题旁显示「已就绪 / 还没填密钥」状态，不用打开对话才知道能不能用。

#### AI 现在能改 PageDye 自己的设置

- ⚙️ **AI 可以提议修改扩展本身的偏好：** 说「把界面主题色换成蓝色」「关掉界面动画」「把暂停快捷键改成 Ctrl+Shift+P」「把网页上的诊断按钮关掉」，AI 会给出一张独立的「PageDye 设置」卡片，列出它打算改哪几项、改成什么值。这张卡片不会自动生效——必须点「更改设置」才写入，因为这类设置是全局的，和"给当前网页配背景"完全是两回事，所以卡片上也明确写着"这一项改的是 PageDye 自己，不是当前网页"。
- 🧷 **只改它提到的那几项：** 一个只谈主题色的提议，不会顺手把快捷键重置成默认值——写入时是合并而不是整体替换。所有值在落盘前会再过一遍与设置页相同的校验（即使聊天记录里已经校验过一次，因为这份数据在存储里存过一轮，而写入的是全扩展都会读的键）。
- 📡 **弹窗和设置页共用同一份应用逻辑：** 新增 `scripts/shared/ai-preferences.js`，两个页面提供的是同一个按钮，"减少动画存在哪个键里"这种事再放两份迟早会不一致。
- 🔄 **设置页现在会跟随外部改动实时重绘：** 界面主题色、调试模式、暂停快捷键从 AI 卡片或弹窗改掉时，已经打开的设置页会立刻同步，而不是停留在旧值直到手动刷新。

#### AI 能配置的东西更多了

- ✨ **AI 可以给背景加动态特效：** 说「加点下雪」「来点粒子」之类，AI 会直接开启对应的动画覆盖层（矩阵雨、粒子、波浪、星空、涟漪、极光、飘雪、打字机等），并按页面用途控制密度和速度——阅读类页面会保持低密度低速度。特效会正确叠加到明暗双色、时段切换、轮播这些模式的每一屏上。
- 🧩 **AI 可以设置兼容模式和背景作用区域：** 你说"背景被挡住了/没生效"时，AI 会把运行模式调到增强（还不行才用强力），并可以指定哪些弹窗、菜单保留自己的背景不被穿透；你说"只给左边这一栏上色"时，它可以把背景限定在页面画像里列出的某个容器上。这两项默认都不动，只有你明确提出才会改。
- 🛡️ **这些新槽位同样受本地校验约束：** 模型编造的、页面上不存在的目标选择器会被丢弃；不认识的特效名当作"关闭特效"处理而不是原样存下；排除选择器里夹带 `{`、`}`、`/*` 的会被拒绝，不会拼进注入网页的样式表。

#### AI 回答改为流式输出

- ⌨️ **回答边写边显示：** 此前一轮对话是转圈等到结束、整段跳出来；现在文字到多少显示多少，长回答不再是一段空等。虽然 AI 返回的是一整个 JSON 对象而不是散文，PageDye 会从还没写完的 JSON 里把"给你看的那半段"实时解出来。
- ⏹️ **停止即停止计费：** 流式一轮走的是一条独立连接，点停止按钮、切换会话、直接关掉弹窗都会真正中断请求，而不是让它在后台继续把 token 写完。
- 🧯 **不支持流式的接口自动回退：** 接口返回的不是事件流时，自动改用原来的一次性请求——能用但不流式，好过直接报错。中途断流或后台被回收时会留下一个可重试的错误，而不是永远卡在转圈。

#### 自定义鼠标指针图片

- 🖱️ **光标形状新增「自定义图片」：** 除了原有的实心球、空心环、发光球、点环组合，现在可以上传自己的图片作为跟随光标，支持拖拽或点击选择。选中后形状网格里那一格会直接显示这张图，一眼能看出选的是什么。
- 📐 **图片会被压到光标该有的尺寸：** 最长边限制 128px、上限 192KB，重新编码为 WebP 并保留透明通道——光标是画在 12–48px 的，透明底又是关键（不透明的方块当指针没法用）。SVG 和 GIF 只要体积在限额内就原样保存，光栅化会毁掉它们各自的意义（矢量的分辨率无关性、GIF 的动画）。非方形图片按比例缩放居中，不会被拉变形。
- 🎨 **颜色滑块此时只影响拖尾：** 图片本身用你上传的颜色，滑块不会给它染色，提示文案已写明。

#### 🐛 Bug 修复

- 🐛 **修复自定义预设色的删除按钮点不动：** 选中态那圈高亮描边是后画的、四周还外扩 4px，正好盖住删除角标的左下角，并把点击事件一并吃掉——结果是**正在使用的那个颜色永远删不掉**。现已给角标提高层级，并让描边本身不参与点击命中。
- 🐛 **修复明暗/轮播/时段缩略图慢一拍：** 卡片是从已保存的配置里读的，而正在编辑的那一格要到保存时才写回去，所以调透明度、换颜色、选图片之后，旁边的缩略图显示的仍是改动之前的样子，要切换配色方案或重开界面才追上。现在会把表单当前的值临时叠加上去再画。弹窗和设置页两边都已修正。
- 🐛 **修复自定义光标没选图片时整个指针消失：** 形状选了「自定义图片」但还没上传时，覆盖层什么都不画，而系统原生指针又是被强制隐藏的，页面上会变成完全没有光标。现在没有可用图片时回退到默认的实心球。同时收紧了图片来源校验：只接受 `data:image/` 和 `http(s)`，从别人分享的备份里导入的其他形式一律当作"没选图片"。
- 🐛 **修复分区间跳转的哈希导航互相打架：** 「预设」和「存储空间」这两个页面各自维护了一份处理 `#section-…` 的代码，和主导航那份争抢同一批元素。现在导航只由 options.js 一处负责。
- 🐛 **修复切换分区后停在上一页的滚动位置：** 打开一个新分区时会出现在半途，像是自己没滚过却已经在页面中间。现在跳转会同时归零两个滚动容器（桌面端是内容区滚动，768px 以下是文档本身滚动，后者可能把短分区整个顶出屏幕）。

#### 🧪 测试

- 新增 32 项测试，覆盖标签页导航与旧深层链接、切换分区归零滚动、减少动画对新增动画的静音、缩略图实时重绘、自定义光标的往返与空图片回退、流式分片与断流重试、不支持流式时的回退、AI 偏好卡片的展示与"只在按下时才应用"、偏好的合并写入与非法值拒绝、以及新增槽位（运行模式 / 目标选择器 / 特效）的注入防护。全部 194 项测试通过。

### ✨ v0.11.3 更新日志

- 🧹 **设置页代码整理，为后续简化布局打基础：** 站点编辑器里滤镜滑块、渐变色标、毛玻璃列表这三处各自独立重复的"渲染 - 收集 - 保存"逻辑，被拆成三个独立模块文件，功能和界面本身没有变化。这是设置页整体简化的第一步——下一步会动到侧边栏的分区划分和一些容易让人摸不清头脑的操作逻辑。

### ✨ v0.11.2 更新日志

- 🎨 **AI 现在能直接给纯色，不用非渐变不可：** 想要"就一个纯色"的时候，AI 会给一个 `solidColor` 而不是硬凑一个两色渐变；聊天卡片里的色块预览也同步支持显示纯色。
- 🌈 **壁纸滤镜补全色相旋转和反色：** 之前 AI 只能调亮度/对比度/灰度这三项，现在色相旋转、反色也在它手里了——"换个色系但别是滤镜糊上去的感觉"这类要求现在能做到。
- 🚫 **可以直接让 AI 把背景关掉：** 明确说"把这个网站的背景关掉/去掉"时，AI 会把背景设成关闭状态，而不是硬凑一个透明或者接近纯白的渐变敷衍过去。
- ⏰ **AI 支持设计按时段切换的背景：** 说"让它跟着白天黑夜换颜色"之类的要求，AI 现在可以直接给出 2-6 个时段（各自的起止时间和配色），不用你自己手动去弹窗里一段段配。
- 🔄 **AI 支持设计轮播背景：** 一次发几张图给它，说"做成轮播"，AI 会把这几张图排成轮播序列，还能设置轮换间隔（每次打开/15 分钟/30 分钟/1 小时/1 天）和顺序（顺序/随机）；轮播的每一屏也可以是纯色或渐变，不一定非要用图。
- 🖼️ **AI 能认出网站已经设置好的背景图，并保留它：** 之前如果网站已经有一张背景图，AI 完全不知道这张图的存在——想"保留这张图，但加个磨砂玻璃"之类的要求只能靠瞎猜重新配一套。现在 AI 对话时会把这张已有的图当作一张"编号图片"提供给模型，模型可以选择继续使用它，而不是每次都用新渐变把它顶掉。
- 🗑️ **AI 对话历史新增"清空全部"：** 会话列表标题栏在「新对话」旁边加了一个一键清空按钮，点击会先弹确认框，没有历史记录时按钮自动隐藏。

### ✨ v0.11.1 更新日志

- 🖼️ **AI 对话支持上传图片：** 输入框左侧新增图片按钮，也可以直接粘贴截图或把图片拖进对话窗口，一条消息最多带 4 张。AI 会先把图片当参考——从里面取色、取氛围来配渐变；如果你明确要求"就用这张图当背景"，或者这张图一看就是壁纸而不是配色板，它会直接把图片本身设成背景，并自动压低透明度、按需加模糊、给正文容器补磨砂玻璃，保证文字仍然可读。主题卡片里图片壁纸会显示缩略图，而不是两块配色色块。
- 👁️ **新增"该模型可以看图"开关：** AI 设置页新增勾选框，没勾选就不显示图片上传入口（按钮、粘贴、拖拽三条路一起关闭），对话里已经带的图片也不会再发出去。Anthropic 的模型默认勾选（都支持读图），OpenAI 兼容接口默认不勾选——不同服务商、不同模型能不能读图无法预先得知，而对一个不支持看图的模型发图片，对方会直接整条拒绝，不是忽略图片。切换服务商会重置为新服务商的默认值。
- 💬 **说"看不清""太亮""太花"，AI 现在按固定顺序调整而不是重新设计：** 先降壁纸透明度，再把正文容器的磨砂调得更实，然后加模糊，最后才是换配色本身；每一步都会说清楚动了哪个参数、动了多大。如果是图片壁纸调了一圈还是看不清，会直接告诉你这张图不适合这个页面，退回用它配的那套渐变。

### ✨ v0.11.0 更新日志

- 💬 **新增 AI 主题对话：** 弹窗新增「AI」标签页，用聊天的方式设计背景。AI 读取当前网页后给出一整套背景配置——浅色/深色两套渐变壁纸，以及把哪些容器变成磨砂玻璃。想改哪里直接用大白话说「再暗一点」「换暖色」，它会在当前这套主题的基础上改，而不是重新生成一套。完整聊天记录按网站分别保存，随时翻回去看。你说的话在气泡里，AI 的回答是页面上的正文（不套气泡），支持 Markdown 渲染——列表、代码块、表格、引用都能正常显示。
- ✏️ **可以改写已经发出去的消息：** 每条消息下方有「编辑」和「复制」，AI 的回答下方还有「重新生成」。编辑后重新发送会把那条之后的对话全部作废重来——后面那些回答，回答的是一个已经不存在的问题。请求失败的那一轮会留在原地并给出「重试」，但不会被当成 AI 说过的话回放给模型。
- ↩️ **先预览后保存：** AI 每次给出的主题以卡片形式展示（浅色/深色渐变色块、磨砂玻璃处数），在弹窗里会立刻画到当前网页上，但不写入配置，点「应用」才保存。连续修改多轮之后放弃，恢复的仍然是你原本的设置，而不是中间某一轮的结果。如果这一轮只是回答了问题、没动配色，卡片会标注「主题未改动」。
- 🔒 **不会发送网页正文：** 发给 AI 的不是网页 HTML，而是一份采样出来的「页面画像」——背景色、强调色，以及几个主要容器的选择器、占屏比例和文字颜色。正文、标题、输入框内容一概不读取，所以邮箱、后台这类页面的内容不会离开浏览器。AI 也不写 CSS，只填固定的槽位（渐变色标、透明度、模糊、磨砂参数），数值和颜色在本地强制钳位，它编造的、页面上不存在的选择器会被丢弃。
- 👁️ **可读性优先于你的要求：** PageDye 只能替换背景，改不了网页自身的文字颜色——深色壁纸配近黑正文就是没法看。因此每个容器当前的文字颜色都会一并告知 AI。你提的要求优先级高于默认的审美倾向，但不高于可读性；无法两全时会以可读为准，并在回答里写清楚。
- 🔑 **自带密钥，支持 Anthropic 与任意 OpenAI 兼容接口：** 可自定义 base URL，因此 DeepSeek、OpenRouter、Groq 以及本地运行的 Ollama / vLLM 都能接入；填基础地址或服务商文档里的完整端点都可以。密钥只保存在浏览器本地，**不会写入导出的备份文件**，也不会出现在任何错误提示中；只允许 https，仅 localhost 可用 http。第一次使用时没填密钥的话，对话页显示的不是一句报错，而是说明加一个「打开 AI 设置」按钮；在别的标签页填好后对话页会自动解锁，不需要重开弹窗。
- ⚙️ **设置页新增「AI 对话」和「AI 设置」两页：** API 服务商、密钥、模型、固定风格偏好从「设置」页里挪出来单独成页。「AI 对话」是完整宽度的对话界面，左侧带会话列表，并且可以从已打开的标签页里挑一个作为要配背景的网页。
- 🧹 **弹窗界面整体收紧：** 顶栏、页脚、底部导航栏、折叠标题、卡片内边距逐项压缩。可视选项区从 360px 增加到 401px，一屏能看到的内容比例从 50% 提升到 61%，底部导航栏从 84px 降到 60px；「此网站 / 全站默认」切换条也窄了一档。
- 🧹 **合并重复的清除入口：** 「清除当前配置」和页脚的「重置」调用的是同一个函数，唯一区别是前者会弹确认框——同一个破坏性操作摆了两个入口，而保护偏偏加在措辞更吓人的那个上。现在只保留「重置」，并给它补上确认。
- 🧹 **移除两条纯解释性提示：** 「应用于所有没有单独设置背景的网站。」和「当前网站没有单独设置…」讲的都是上方切换条已经表明的事，却在每次打开时占掉两行。由页面规则控制、或被规则排除时的提示保留——那两种情况光看界面是推不出来的。
- 🐛 **修复弹窗里来路不明的青绿色：** 弹窗样式表里的兜底配色是一套青绿色，而默认主题色其实是中性灰——自从主题系统搬进 JS，这两者就没对上过。首帧、以及主题没能应用时看到的绿色色块和胶囊就是它。同时修复了悬停/按下的着色**从来不跟随主题色**：这两个变量在样式表里用了 14 处，JS 却从没写过它们，不管你选什么主题色它们都停在默认值上。

### ✨ v0.10.1 更新日志

- 📦 **视频体积上限从 15MB 提升到 40MB：** 同步调高了存储 schema 的单值上限（32M → 64M 字符）和备份文件上限（64MB → 128MB）。40MB 是当前"base64 存入 storage.local"方案的实用极限——base64 会把体积膨胀 4/3，再大就会逼近 Chrome 扩展消息 64MB 的上限（保存到 URL 规则时整份配置要经消息传给后台）。注意：视频越大，配置了该视频的网站每次打开时读取配置的开销越高，长视频仍建议先压缩。
- 🐛 **修复"选择背景"选中状态的高亮色块仍会消失的遗留问题：** 新增"视频"选项后色块定位规则的同步修复本次一并生效。
- 🐛 **视频/图片导入失败的提示更明显、更持久：** 此前无论是设置页还是弹窗，选错文件或体积超限的报错都和普通"已保存"提示一样，2 秒后自动消失，容易错过。现在设置页的报错提示改为红底常驻，直到点击才关闭；弹窗的状态灯和文字也统一变红显示。视频体积超限的提示还会带上具体文件大小（"你的文件是 X MB，上限是 40MB"），不用自己再去猜超了多少。同时修复了弹窗里图片导入失败错误样式和视频不一致的问题（图片失败此前仍显示旧的灰色提示）。

### ✨ v0.10.0 更新日志

- 🎬 **新增本地视频壁纸：** 弹窗和设置页的"选择背景"新增"视频"类型，可以用本地 MP4 / WebM 文件作为背景循环播放，和图片一样支持透明度、模糊、亮度/对比度/灰度/色相/反色滤镜，以及固定位置、填充方式（覆盖/包含/拉伸）等既有控件，不用重新学一套设置。
- ⏱️ **视频体积与解码校验：** 单个文件最大 15MB（没有压缩方案，体积较大的视频建议先自行裁剪或压缩再导入）；选择文件时会先用真实解码验证浏览器能不能播放，遇到不支持的编码会直接提示，而不是先存进去、之后才发现放不出来。
- 🔋 **播放性能：** 视频统一静音、自动循环播放；标签页切到后台会自动暂停解码，切回来自动恢复，不会在看不见的时候持续耗电耗资源。
- 🧩 **已知限制：** 暂不支持"背景选择器"（只对网页某个区域生效的模式）——底层实现是纯 CSS 伪元素，结构上放不进 `<video>` 标签，视频背景只能用于全页模式；也暂不支持远程视频地址，只能选本地文件。
- 🗂️ **存储空间管理支持视频：** 此前"存储空间管理"页面只认图片，视频文件完全不出现在统计里、不参与查重、也清理不掉。现在视频会单独计数（不影响原有的图片体积统计和"减小图片体积"功能）、参与重复检测和"清理未引用"，列表里也有独立缩略图（为避免多个视频同时解码卡顿，缩略图只显示静态首帧，不自动播放）。
- 🛡️ **安全修复——网站规则/磨砂玻璃可被注入额外 CSS 规则：** 内容脚本把"背景选择器"目标选择器、磨砂玻璃选择器、图片滤镜（亮度/对比度等）数值直接拼接成文本、整段写进注入到网页里的 `<style>` 标签。如果这些字段来自一份别人分享、你导入的配置备份，且被刻意塞进了 `{`、`}` 或注释符 `/*`，就可以提前闭合 PageDye 自己的 CSS 规则、在你正常浏览的网页里插入攻击者指定的样式规则（可用于伪造遮罩、钓鱼式界面）；滤镜数值如果不是合法数字，也会被原样拼进样式字符串。修复方式：选择器一旦包含 `{`、`}`、`/*` 直接拒绝该条设置，滤镜数值统一强制转换为数字、非法值回退默认值，两处校验分别加在了 schema 层和实际拼接的位置，双重保险。
- 🛡️ **安全修复——Lite（userscript）版设置面板 XSS：** 设置面板拼颜色/数值输入框的 HTML 时，`rangeRow`/`colorRow`/渐变色标编辑器这三处唯独漏掉了对字段值的转义（同文件其他地方都有）。导入一份配置分享文件后，其中的颜色/数值字段可以闭合输入框的 HTML 属性、插入类似 `<img onerror=...>` 的标签；由于设置面板会在你每次打开新网页时自动重新渲染，不需要手动点开设置，插入的代码会在你之后访问的每个网站页面里执行。已为这三处补齐转义。
- 🛡️ **安全修复——调试面板伪造事件可执行脚本：** 开发者选项"调试模式"的网络请求面板通过浏览器全局事件从页面主环境接收捕获到的请求数据，之前用 `Object.assign` 合并时顺序写反了，导致伪造的同名事件可以覆盖面板内部生成的请求编号，而这个编号又未经转义直接拼进面板的 HTML，二者结合可以在面板里执行任意脚本。已经调整合并顺序禁止外部覆盖编号，并给相关输出统一转义。该入口默认关闭，只有主动打开"调试模式"并勾选"开启捕获"才涉及，日常使用不受影响。
- 🐛 **修复"选择背景"选中状态的高亮色块消失：** 新增"视频"选项后，原本按固定 5 个选项写死的高亮色块定位规则没有同步更新为 6 个，导致不管选中哪个类型都看不到高亮标记。现在色块定位规则已同步，并补充了自动化测试防止同类问题再次出现。

### ✨ v0.9.5 更新日志

- 🗂️ **合并"问题诊断"到"设置"页：** 移除独立的"问题诊断"导航项，"调试模式"改为嵌套在"设置"页内、随"高级模式"一起出现的"开发者选项"分组，两者的从属关系不再需要靠猜测判断。同时在"高级模式"开关下方新增"同时还会解锁"清单，一次列出该开关会影响到的全部功能（页面规则、批量工具、自定义动效、每站点自定义 CSS/选择器、设置页背景与动画开关、存储详细工具、开发者选项）。
- 🎨 **设置页改用统一的 Material You 分层配色卡片系统：** 此前每个卡片各自拼凑边框、圆角、背景色组合，界面显得零散。现在按 Material You 表面层级（页面 → 卡片 → 内嵌区块 → 输入框）逐级递进色调区分层次，去掉了绝大多数实体分割线；选中态、聚焦态改用描边高亮而不是常驻边框。
- 🧱 **重做按钮、表格、标签页、侧边导航为 Material You 组件规范：** 按钮拆分为 Filled / Outlined / Tonal / Text / 危险五种规范样式并统一交互态；表格改为低强度分割线加顶部色块表头；网站编辑页顶部的"壁纸 / 磨砂玻璃"切换与侧边导航都改为胶囊选中态。
- 🐛 **修复"清除全部本地数据"按钮与卡片颜色糊在一起：** 危险按钮此前与所在的警示卡片使用了同一种底色，视觉上几乎融为一体。现在改为实心警示色按钮，与卡片背景形成清晰对比，深色模式下同步修正。

### ✨ v0.9.4 更新日志

- 🐛 **修复切换回“此网站”时设置被清空：** 此前切换到“全站默认”会立即删除当前网站的独立配置，导致切换回“此网站”后原有设置丢失。现在切换标签页仅用于查看/编辑对应目标，不再附带删除副作用，网站的独立背景配置会被保留。
- 🧹 **弹窗新增“清除当前配置”：** 在“此网站 / 全站默认”切换提示旁新增按钮，可直接清除当前正在编辑的目标（此网站或全站默认）的背景配置；点击后会先弹出确认提示，避免误触。
- 📱 **设置页小屏幕改为侧边栏抽屉：** 此前窄屏下顶部导航会挤压成一条不显示滚动条的横向可滑动列表，容易让人以为只有这几个选项、看不出还能滑动。现在改为汉堡按钮 + 侧边抽屉：点击后从左侧滑出完整的竖直导航列表（含遮罩层，点击遮罩、关闭按钮或选择任意页面均会收起）。桌面端布局不受影响。
- 🎨 **抽屉样式改为 Material You 观感：** 修复抽屉底部因残留的旧版圆角规则而出现的错位缺角；去掉顶部栏、汉堡按钮、抽屉页脚等处的多余分隔线，导航项改为胶囊形状，版本号改为色块徽标，图标按钮改为无边框圆形，整体更贴近 Material You 的分层色调风格。

### ✨ v0.9.3 更新日志

- 🧹 **彻底清除本地数据：** “清除所有网站设置”现在会清空扩展的全部本地存储，包括全站默认背景、已保存图片、界面偏好与站点配置，不再残留旧背景。
- 🖼️ **修复背景模式切换：** 修复图片背景仍被动态波浪覆盖、网站背景编辑器切换后内容不显示的问题；图片预览的半透明遮罩改为固定中性灰色，不再随界面主题变色。
- 🔀 **网站与全站默认可随时切换：** 高级设置的“编辑网站背景”新增顶部“此网站 / 全站默认”切换；选择全站默认会移除当前网站的独立覆盖并立即继承默认配置，切换期间的待保存操作也不会写错目标。
- ✨ **优化设置交互：** 修复无标题分段选项的垂直居中问题，并提升手风琴快速展开/收起时的动画稳定性，减少闪烁。

### ✨ v0.9.2 更新日志

- ✍️ **新增网页文字编辑：** 在“开发者控制”中启动文字拾取器，悬停高亮目标文字后点击，即可在悬浮编辑卡片中修改内容；修改会即时反映到页面。
- 💾 **文字修改可持久保存：** 保存操作会等待本地存储写入完成，并按完整页面 URL 保存。再次打开或刷新同一页面时会自动恢复；对延迟渲染页面会在页面加载完成后再次应用，减少被页面渲染覆盖的情况。
- 🎨 **优化文字编辑卡片：** 重新设计为居中、半透明的深色浮层，使用更紧凑的暗色输入框、清晰的说明文字和更明确的保存/取消操作。

### ✨ v0.9.1 更新日志

- 🎚️ **新增自定义光标透明度：** 光标设置中新增透明度滑块，可统一调节光标主体（圆点/圆环）与拖尾效果的整体透明度。
- 🐛 **修复点环光标与拖尾的渲染偏移：** 点环组合样式的内部圆点与外圈圆环，此前因浏览器对圆环边框宽度的取整方式与圆点不一致，渲染中心会有约 1px 的偏差，导致圆点看起来没有居中；同时开启拖尾并启用平滑跟随时，拖尾使用的是未经缓动的原始鼠标坐标，会与缓动中的光标本体脱节。现在两个元素统一改为基于最终渲染尺寸居中，拖尾也统一取用光标本体的实际位置，避免了取整误差和位置不同步。

### ✨ v0.9.0 更新日志

- 🛡️ **修复 URL 规则并发保存可能丢失设置的隐患：** 此前弹窗、设置页和网页内容脚本各自直接读取、修改、写回同一份 URL 规则数据，几乎同时保存时后写入的会静默覆盖先写入的（包括幻灯片自动轮换这类非用户主动触发的写入）。现在统一经由后台服务的串行写入队列处理，逐个应用每次修改，从根本上消除了这类丢失更新。
- 🧹 **抽取弹窗与设置页的共用代码：** 颜色计算（十六进制转换、HSL、主题色推导）、手风琴展开收起动画、代码编辑器（语法高亮、括号自动补全、Tab 缩进）等此前在弹窗和设置页中各自维护的实现，统一抽取为共享模块，减少约500行重复代码。
- 🎨 **合并重复的样式规则：** 弹窗与设置页的 CSS 经过多轮界面改版后堆积了大量重复选择器。这次逐一分析每组重复声明，只合并了确认不会因层叠顺序或选择器优先级冲突而改变实际显示效果的部分；有真实冲突风险的部分保持原样，并用浏览器对全部界面元素做了改动前后的逐属性比对，确认零视觉差异。
- 🌐 **统一中文翻译回退顺序：** 修正弹窗与设置页文案在缺少主翻译时的回退优先级不一致问题，避免同一词条在两个界面显示不同的中文。
- ✅ **补充真实行为测试：** 新增覆盖弹窗/设置页启动流程、保存交互、后台存储写入队列的自动化测试，不同于此前主要依赖正则匹配源码文本的测试方式，为后续维护提供更可靠的回归保护。

### ✨ v0.8.7 更新日志

- 🎛️ **优化分段控件动画：** 弹窗与设置页的模式切换、背景类型等分段控件现在使用平滑滑动的选中背景，切换状态更连贯。
- 🎨 **统一设置页表面配色：** 设置页背景、主面板和侧边栏改为跟随系统明暗模式与界面主题；移除容易造成配色冲突的背景颜色输入，同时继续支持页面和容器背景图片。
- 🪟 **调整弹窗顶部投影：** 将原先集中在左上角的阴影改为更明显的顶部中央投影，并分别优化浅色与深色模式下的强度。
- 🖱️ **改善自定义光标：** 默认光标颜色由粉色改为蓝色；按钮等可交互元素上的悬停放大加入平滑动画，并通过重绘实际尺寸避免缩放后的边缘模糊。

### ✨ v0.8.6 更新日志

- 🎨 **统一设置页 UI：** 修复了“高级模式”与“问题诊断”卡片样式不一致的问题，现在它们都使用了更加精致的面板设计并统一了 Material You 3 的大圆角。
- 🔍 **修复搜索框样式：** 修复了设置页中“已保存的网站”搜索框在部分主题下内边距过大的问题。
- 🎛️ **统一所有下拉框样式：** 重新设计了设置页中所有的下拉选择框（包括新建规则、预设操作、群组筛选等），使其具有统一的 12px 圆角、无边框与更粗的字重，完全与轻量级弹窗（Popup）内的预设下拉框在视觉上保持一致。
- 🖼️ **修复并优化扩展图标：** 修复了侧边栏和关于界面的扩展图标（因为自带渐变背景导致）出现的“毛边”问题，并移除了多余的阴影；同时将“关于 PageDye”的图标调大到 80x80，使其更加美观醒目。
- 🏷️ **优化版本号标签：** 重新设计了侧边栏左下角的版本号标签，为其添加了更宽的内边距和强调色背景，使其显示为药丸形状的 Badge，解决了右侧数字超出背景的问题。

### ✨ v0.8.5 更新日志

- 优化批量工具：“批量工具”重命名为“预设”，无需高级模式也可访问；精简未开启高级模式时的界面，隐藏部分复杂的批量管理功能。
- 修复并优化 UI：修复了高级模式设置中开关的渲染比例问题，更贴合整体的界面风格。
- 补全多语言支持：在中文语言包中补充了遗漏的“已保存的网站”相关提示词条。

### ✨ v0.8.4 更新日志

- 默认隐藏专业设置和技术术语，大幅精简和优化中英文文案，只保留核心易懂的操作逻辑。
- 引入“高级模式”（Advanced Mode），在独立的设置页面中提供开关。开启后，将解锁页面规则、批量管理工具、自定义动效编辑、详细存储明细及专家微调选项。
- 优化移动端和桌面端各选项页面、配置面板、兼容性设置和下拉选择框的交互样式，适配不同的屏幕宽度。
- 新增合并测试用例，校验高级模式下的界面过滤、简化逻辑与相关权限。

### ✨ v0.8.3 更新日志


- 新增存储空间管理：显示浏览器精确总占用、本地图片占用、可回收空间、唯一图片数，以及各站点、URL 规则、预设、全站默认和控制面板外观的占用明细。
- 新增图片审计工具：支持按图片大小或来源排序，筛选并定位重复图片，识别当前模式不再引用的历史图片字段，并可在确认后批量清理。
- 新增已有图片重新压缩：提供高画质、均衡和更小体积三档 WebP 重压缩；自动跳过 GIF/SVG，仅在结果确实更小时写回，并显示处理进度与节省空间。
- 优化大存储量体验：图片列表采用按需加载和渲染上限，显示配额进度、重复数据浪费与备份大小估算，存储变化后自动刷新。
- 完整备份和选中站点备份现在都会在下载前提示预计大小及包含的本地图片引用数量。

### ✨ v0.8.2 更新日志

- 新增完整配置预设：弹窗内重做预设界面，可一键保存当前站点并快速应用；内置浅色极光、深色极光、粒子日间、粒子夜间 4 个预设，设置页支持自定义预设和配置缩略图。
- 新增站点分组与批量操作：可把预设应用到多个站点、复制站点配置、创建和维护分组。
- 新增选中站点导入/导出：导入前可逐项勾选，不再需要恢复整份备份。
- 备份格式升级为 schema v3，纳入自定义预设与站点分组，并继续兼容 schema v1、v2 和旧版原始 JSON。
- 修复设置页在系统深色模式下仍使用浅色 HTML 根背景的问题；默认主题和旧版默认配色会跟随系统实时切换，自定义背景保持不变。

### ✨ v0.8.1 更新日志

- 新增按 URL 匹配的有序规则：支持 hostname、精确网址、路径前缀和 `*.example.com` 通配子域名。
- 新增排除规则、规则临时禁用，以及拖动排序；列表中越靠上的规则优先级越高。
- URL 规则可以保存独立背景，同一网站的不同页面可使用不同配置；原有 hostname 配置继续作为兼容回退。
- 备份格式升级为 schema v2 并包含 URL 规则，同时保持对 schema v1 备份的导入兼容。

### ✨ v0.8.0 更新日志

- 🧬 **修复内容脚本重复注入和生命周期泄漏：** popup 自动保存、设置页通知和三类元素拾取器统一改用完整依赖加载器，只在页面缺少运行时时才重新注入；content/effects/cursor 会清理旧的 `requestAnimationFrame`、MutationObserver、定时器、iframe 和事件监听器，避免扩展重载或反复保存后性能持续下降。
- 🖼️ **修复全站默认幻灯轮播：** 使用全站默认背景时，幻灯片轮换会正确更新全站默认存储键，不再意外创建或覆盖当前域名配置。
- 🧰 **调试工具改为按需加载：** 默认页面不再预先注入调试面板和 MAIN world 网络桥；只有用户主动启用调试模式时才由新增的 MV3 service worker 加载，并移除了不必要的 `activeTab` 权限和无效公开资源声明。
- 💾 **重做备份、导入和清理边界：** 新增 schema v1 备份格式并兼容旧版 JSON；只接受站点配置、全站默认、自定义动效和自定义配色，增加文件大小、字段、嵌套背景、危险键名和保留键校验；导出也会验证最终 JSON，“清空所有网站”只删除站点配置，不再误删动效、配色、调试状态或总开关。
- 🗜️ **加强本地图片存储保护：** 上传图片会按比例缩放并尝试多档 WebP 质量，分别限制输入文件与最终存储大小；解码或 Canvas 压缩失败时不再无条件保留巨大原图，popup 和设置页会直接显示失败原因，data URL 也不再被静默截断。
- 🛡️ **隔离并修复自定义 Canvas 动效：** 用户代码改在独立扩展沙箱 iframe 中编译和运行，无法访问扩展 API，外部连接和资源加载受 CSP 限制，严格 CSP 网站也能正常显示；父子消息验证来源窗口与随机 channel，补充启动/请求超时、释放重建、鼠标转发和运行时错误上报，设置页的代码与 URL 实时预览可以安全往返切换。
- 🚨 **让代码导入确认先于执行：** 单个动效和整体备份都会先显示 JavaScript 风险提示，确认后才在沙箱中验证代码并写入存储；同时限制动效名称、代码、导入文件和总数量（最多 100 个），无效代码不再静默回退到内置 Waves。
- 🌐 **收紧 URL 动效背景：** 只接受 HTTPS 地址（`localhost` 和回环地址可使用 HTTP），内容页与设置页预览 iframe 均加入 sandbox、无 referrer 和受限权限；交互模式保存前再次确认，并明确提示部分网站会因 CSP 或 `X-Frame-Options` 无法嵌入。
- ♿ **改善设置页可访问性：** 确认弹窗补充 dialog/ARIA 语义、初始焦点、Esc 关闭、Tab 焦点循环、点击遮罩关闭和关闭后焦点恢复；popup 与设置页重置按钮显式声明 `type="button"`。
- 🧩 **补齐正式扩展图标：** 从官方 `Designer.png` 生成有效的 16/48/128 像素 PNG，manifest 使用对应尺寸，不再打包 0 字节图标或以超大原图代替小图标。
- ✅ **加强工程质量门禁：** 新增共享存储校验模块、16 项行为与资源测试、全项目 JavaScript 语法检查、manifest/发布包资源检查、图标尺寸检查和版本同步检查；新增 push/PR CI，标签发布前执行完整检查，并确保沙箱运行时进入正式安装包。
- 📱 **继续优化 PageDye Lite：** 四种壁纸模式保持紧凑四列图标布局并补充无障碍标签，高级页隐藏无关目标切换控件，时段管理继续保留响应式布局。
- 🔐 **修正文档中的隐私说明：** 明确配置和图片只保存在本地、PageDye 不含遥测；只有用户主动配置远程图片或 URL 背景时，浏览器才会访问对应地址，并补充备份、代码沙箱、URL iframe、调试模式与权限用途说明。

### ✨ v0.7.24 更新日志

- 🎛️ **重做 Lite 壁纸模式切换布局：** 四个模式按钮改为等宽响应式网格，图标和文字完整居中，窄屏自动切换为两列，避免“幻灯轮播”被挤出面板。
- 🕒 **完善 Lite 时段多态样式：** 为清晨、正午、黄昏、深夜等时段按钮补充卡片、悬停和选中状态，时段名称与时间范围层级更清晰。
- 🧹 **优化 Lite 时段管理操作：** “添加新时段”和“删除当前时段”改为紧凑、等宽且自适应的操作按钮，修复文字竖排和删除按钮尺寸异常。

### ✨ v0.7.23 更新日志

- 🕒 **补全 Lite 时段多态图标：** 修复轻量版调用未定义时钟图标、导致“时段多态”模式按钮没有正常显示图标的问题。

### ✨ v0.7.21 更新日志

- 🖼️ **新增图片拉伸尺寸：** 背景尺寸新增“拉伸（Stretch）”，可将图片填满整个背景区域；扩展弹窗、设置页和用户脚本均已支持，预览同步显示效果。
- ⚙️ **兼容模式调整：** 深度兼容设置改为普通、增强与强兼三档运行模式，便于按网站情况选择。

### ✨ v0.7.20 更新日志

- 🔌 **新增 PageDye 总开关：** 在 popup 底部加入全局一键开关，关闭后会停止所有页面上的 PageDye 功能，包括壁纸、磨砂玻璃、自定义光标、深度兼容模式和调试模式。
- 📴 **彻底关闭页面引擎：** 总开关关闭后会清理 PageDye 注入的 DOM/style、页面监听、定时器、光标层、深度兼容状态，并让 debug 网络捕获仅在真正启用时才 patch `fetch/XHR`，关闭后恢复原生方法。
- 🪟 **新增插件关闭提示层：** 手动关闭插件后，popup 会显示与安全限制一致风格的居中提示；点击“打开 PageDye”即可恢复。
- 🧭 **调整 popup 底部布局：** 插件总开关移到底部，状态、重置、版本号和 GitHub 入口整条工具栏放在总开关下面，避免占用首屏主要设置空间。

### ✨ v0.7.19 更新日志

- 🎨 **界面主题色现在也会染色背景和控件：** 之前"界面主题色"只改变按钮、徽标等强调色，弹窗和设置页自身的背景、卡片、悬浮容器、分割线、文字颜色仍然是固定灰调；现在选中的主题色会像真正的 Material You 动态取色一样，让这些中性表面统一带上一层与强调色协调的淡淡色调（黑白灰仍保持接近纯灰，鲜艳色则观感更统一）。设置页里手动指定的"外部背景"与"面板背景"颜色不受影响，依旧以你的自定义颜色为准。

### ✨ v0.7.18 更新日志

- 🛡️ **新增强兼模式：** 在桌面端弹窗的“深度兼容模式”中新增可选的“强兼模式”，用于对付会反复篡改 DOM 或样式、试图遮挡 PageDye 背景的顽固网站；开启后会更高频地检测 DOM/style/class 变化并恢复 PageDye 覆盖。
- 🔧 **增强深度兼容抗回滚能力：** 强兼模式会在开启后的短时间内高频压制，持续重建被删除的 PageDye 根节点/样式，反复重写已处理遮挡层，并额外压制大面积遮挡元素的 `::before` / `::after` 伪元素背景。
- 🎛️ **优化弹窗布局与滚动体验：** 收紧手风琴标题高度，让设置项更紧凑；状态栏改为随内容滚动；底部导航不再被滚动条遮挡；中间内容区滚动条默认隐藏但仍可滚动。
- 🧩 **优化强兼模式可用状态：** 未开启“为此网站启用”时，强兼模式整组会显示为灰色禁用状态，避免误触。

### ✨ v0.7.17 更新日志

- 🛡️ **受浏览器安全限制的页面现在会明确禁用：** 当 PageDye 无法在当前标签页运行时（例如 Chrome 受保护页面、Chrome Web Store、未授权的本地文件页等），弹窗会自动切换为灰色禁用状态，不再让用户误以为设置已经生效。
- 🧭 **新增居中的安全限制提示层：** 在无法运行的页面中，弹窗会显示“由于浏览器安全限制无法运行”的提示卡片；提示层固定在弹窗可视区域中心，并锁定滚动，避免滚动时提示跑偏。
- 🧪 **新增严格 CSP 本地测试站点：** 添加 `test-sites/strict-csp-hostile`，用于手动测试严格 CSP、禁止 iframe、厚背景层和主动移除注入节点等极端兼容场景。

### ✨ v0.7.16 更新日志

- 🎨 **桌面端弹窗切换到 Material You 3 风格：** 重新整理桌面端 popup 的颜色、圆角、Surface 层级、按钮、卡片、输入框、滑块和底部导航，让整体观感更贴近 Material You 3，并同时适配浅色/深色模式。
- 🛠️ **修复弹窗 UI 细节问题：** 修复顶部“此网站 / 全站默认”分段按钮文字没有垂直居中的问题；移除磨砂玻璃页底部多余空白；收窄手风琴卡片宽度；为底部导航栏上方增加小圆角。
- 🧹 **清理历史样式残留：** 移除 popup 样式文件中一段残留的无效 CSS，避免后续样式解析出现异常。

### ✨ v0.7.15 更新日志

- 🎨 **新增界面主题强调色：** 弹窗和设置页新增"界面主题色"选择器，提供黑白灰、红、粉、紫、靛蓝、蓝、青、蓝绿、绿、橙 10 种预设色，也可以自定义任意颜色；只改变 PageDye 自身界面的按钮/高亮等颜色，网页背景颜色不受影响。
- 🛠️ **修复删除单个网站配置时提示文案错误的问题：** 之前误用了"清除全部网站"的提示语，现在会正确显示"网站配置已清除"。

### ✨ v0.7.14 更新日志

- 🎨 **动效可以叠加在背景上了：** "背景类型"重新设计为"背景（无 / 纯色 / 渐变 / 图片，四选一）+ 动效独立开关"的组合模式——现在可以在纯色、渐变或图片背景上叠加一层动画 Canvas 壁纸，也可以像以前一样单独把动效当背景用；矩阵雨、雨滴等带拖尾效果的动效叠加时会正确透出下层背景，不再整片糊成纯色。
- 🖱️ **新增自定义光标：** 可在任意网站启用自定义光标，提供实心球、空心环、发光球、点环组合四种样式，颜色、大小、悬停放大倍数均可自定义；默认精确 1:1 跟随鼠标，也可以开启"平滑跟随"体验缓动效果，并支持渐隐 / 彗星 / 闪烁三种鼠标拖尾。
- 🧊 **磨砂玻璃支持自定义底色：** 每个磨砂玻璃元素都可以单独设置底色和透明度，不再只能使用固定的白/黑半透明遮罩；适合给卡片、侧栏、搜索框做更贴合壁纸的玻璃拟态效果。
- 🧭 **弹窗与设置页编辑器同步优化：** 背景选择改成更直观的"无 / 纯色 / 渐变 / 图片 / 动效"入口，动效叠加状态会在预览卡片上显示小圆点提示；旧版保存的"动效背景"会自动迁移为"无背景 + 动效叠加"，原有配置不会丢。
- 🎛️ **设置页视觉升级：** 选项页补齐与弹窗一致的背景编辑控件、颜色预设、模式切换和光标设置，并整体调整为更统一的 Material Design 3 风格，浅色/深色主题的色彩、按钮、卡片、表格和表单状态都更协调。
- 🛠️ **修复 PageDye Lite 底部导航切换闪烁问题并优化渲染性能：** 之前切换底部标签页（壁纸 / 磨砂玻璃 / 高级设置）时会重建整个控制面板，导致导航栏跟着闪烁；现在只更新变化的标签页内容区域，并清理重复残留的淡入动画，低性能设备上切换和编辑设置更跟手。
- 🧹 **下线官网体验版小组件：** 移除了官网上的"体验版"网页小组件，避免移动端访问时因为在线演示过重而卡顿；下载与功能说明保留在官网和 Releases 中。

### ✨ v0.7.13 更新日志

- 🎨 **纯色预设焕新：** 浅色、深色预设各自独立成组，一共 28 种颜色，深色系不用切换系统主题也能选到耐看的深色背景；点击"+"可以把当前颜色存成自定义快捷色块，随时可移除。
- 🌈 **重做全部 12 款渐变风格：** 换成了日暮、深渊、翡翠森林、北极光等更协调耐看的配色，替换掉了偏跳脱花哨的旧配色。
- 🛠️ **修复弹出窗口背景类型切换 bug：** 选择"图片"或"动效"背景类型后，下方的具体设置项（特效样式、密度、速度、上传等）不显示的问题。
- 🛠️ **修复控制面板深色模式下文字不可见问题：** 在"外观"页自定义了浅色背景、但系统/浏览器处于深色模式时，"已配置网站"列表里的域名和背景类型标签会显示成看不见的白字叠白底。
- 🛠️ **修复颜色输入框字体问题：** 部分系统下颜色值输入框会错误显示为宋体，现在统一使用等宽字体。
- 🛠️ **修复时段模式类型标签空白问题：** 使用"自定义时段"模式的网站，在"已配置网站"列表里的类型标签之前会显示空白，现在会正确显示"时段 (N)"及对应的多色预览图标了。

### ✨ v0.7.12 更新日志

- 📱 **Lite 版重构 Material Design 3 底部导航：** 彻底移除了 Lite 版过时的顶部分段按钮，全面对齐桌面端的 MD3 风格！现在拥有了“壁纸”、“磨砂玻璃”、“高级设置”三个底部独立标签页，视觉与操作体验全面统一。
- 🎨 **深度兼容模式 UI 优化：** 精简了繁杂的提示文本，换用了更加清爽干练的布局与标签。
- 🎯 **支持连续拾取多元素：** 在深度兼容模式下点击“拾取元素”后，可以连续点击屏幕上的多个元素来一次性排除了！每次成功拾取时拾取框会闪烁绿光提示（按 Esc 或右键完成拾取）。
- 🔘 **更加精致的模式图标：** 壁纸模式切换按钮去掉了冗长的纯文本描述，换用带有精致小图标的圆角药丸式分组按钮。
- 🛠️ **UI 布局与样式修复：** 修复了时段模式下控件布局的溢出问题，恢复了深色兼容模式列表中拾取与删除按钮的正确外观，并统一了输入框样式。
- 🌐 **本地化：** 拾取提示等文本均已完全汉化。

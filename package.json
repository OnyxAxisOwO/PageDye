{
  "name": "pagedye",
  "private": true,
  "version": "0.12.1",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && node tools/check.mjs"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
